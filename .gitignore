# Virtual environments
.venv/
venv/
env/

# Python byte-code and caches
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Raw dataset archives (downloaded from PhysioNet / Kaggle)
archive/
*.csv.gz

# IDE and OS artifacts
.vscode/
.idea/
.DS_Store
Thumbs.db

# LiveGuard auth runtime artifacts -- NEVER commit user credentials or the
# token-signing secret (committing the secret allows forging valid tokens).
data/.liveguard_secret
data/liveguard_users.db*

# OpenCode harness artifacts (not part of the project)
.opencode/
opencode.json